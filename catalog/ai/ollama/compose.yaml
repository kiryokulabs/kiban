services:
  ollama:
    image: ollama/ollama:latest
    restart: unless-stopped
    ports:
      - "${KIBAN_OLLAMA_PORT:-11434}:11434"
    volumes:
      - ollama_data:/root/.ollama
volumes:
  ollama_data:
