services:
  langflow:
    image: langflowai/langflow:latest
    restart: unless-stopped
    ports:
      - "${KIBAN_LANGFLOW_PORT:-7860}:7860"
    volumes:
      - langflow_data:/data
volumes:
  langflow_data:
