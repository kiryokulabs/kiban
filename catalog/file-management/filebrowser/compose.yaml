services:
  filebrowser:
    image: filebrowser/filebrowser:latest
    restart: unless-stopped
    ports:
      - "${KIBAN_FILEBROWSER_PORT:-80}:80"
    volumes:
      - filebrowser_data:/data
volumes:
  filebrowser_data:
