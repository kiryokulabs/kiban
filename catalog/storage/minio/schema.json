{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "MINIO_ROOT_USER": {
      "type": "string",
      "default": "${KIBAN_MINIO_ROOT_USER:-kiban}"
    },
    "MINIO_ROOT_PASSWORD": {
      "type": "string",
      "default": "${KIBAN_MINIO_ROOT_PASSWORD:-kiban12345}"
    }
  }
}
