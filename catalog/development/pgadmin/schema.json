{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "PGADMIN_DEFAULT_EMAIL": {
      "type": "string",
      "default": "${KIBAN_PGADMIN_EMAIL:-admin@example.com}"
    },
    "PGADMIN_DEFAULT_PASSWORD": {
      "type": "string",
      "default": "${KIBAN_PGADMIN_PASSWORD:-kiban}"
    }
  }
}
