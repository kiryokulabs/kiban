{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "KIBAN_TOOLJET_PORT": {
      "type": "string",
      "default": "${KIBAN_TOOLJET_PORT:-3000}"
    },
    "KIBAN_TOOLJET_DB_PASSWORD": {
      "type": "string",
      "default": "${KIBAN_TOOLJET_DB_PASSWORD:-tooljet}"
    }
  }
}
