services:
  tempo:
    image: grafana/tempo:2
    restart: unless-stopped
    command: -config.file=/etc/tempo.yaml
    ports:
      - "${KIBAN_TEMPO_PORT:-3200}:3200"
    volumes:
      - tempo_data:/data
volumes:
  tempo_data:
