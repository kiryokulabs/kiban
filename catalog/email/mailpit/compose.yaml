services:
  mailpit:
    image: axllent/mailpit:latest
    restart: unless-stopped
    ports:
      - "${KIBAN_MAILPIT_PORT:-8025}:8025"
    volumes:
      - mailpit_data:/data
volumes:
  mailpit_data:
