{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "KIBAN_COUCHDB_USER": {
      "type": "string",
      "default": "${KIBAN_COUCHDB_USER:-admin}"
    },
    "KIBAN_COUCHDB_PASSWORD": {
      "type": "string",
      "default": "${KIBAN_COUCHDB_PASSWORD:-kiban}"
    }
  }
}
