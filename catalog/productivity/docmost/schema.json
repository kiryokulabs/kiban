{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "KIBAN_DOCMOST_DB_PASSWORD": {
      "type": "string",
      "default": "${KIBAN_DOCMOST_DB_PASSWORD:-docmost}"
    },
    "KIBAN_DOCMOST_APP_SECRET": {
      "type": "string",
      "default": "${KIBAN_DOCMOST_APP_SECRET:-changeme}"
    }
  }
}
