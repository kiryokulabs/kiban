{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "KIBAN_KIMAI_DB_PASSWORD": {
      "type": "string",
      "default": "${KIBAN_KIMAI_DB_PASSWORD:-kimai}"
    },
    "KIBAN_KIMAI_DB_ROOT_PASSWORD": {
      "type": "string",
      "default": "${KIBAN_KIMAI_DB_ROOT_PASSWORD:-root}"
    },
    "KIBAN_KIMAI_SECRET": {
      "type": "string",
      "default": "${KIBAN_KIMAI_SECRET:-changeme}"
    }
  }
}
