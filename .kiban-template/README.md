# Local Kiban directory template

Kiban runtime data belongs in the user's home directory, not in the repository:

```text
~/.kiban/
  config/
  database/
  plugins/
  logs/
  cache/
```

This template documents the expected layout for installers and development tooling.
